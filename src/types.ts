import { User } from './types/index';

export interface Bet {
  title: string;
  description: string;
  distance: string;
  pace: string;
  probability: string;
  odds: string;
}

export type { User }; 